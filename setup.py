from setuptools import find_packages, setup

setup(
    name="fuser",
    version="0.1.0",
    description="A modality-aware fusion project.",
    author="Yusuf Shihata",
    packages=find_packages(),
    install_requires=[],
    python_requires=">=3.6",
)
